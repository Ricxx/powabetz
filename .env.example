# Copy to .env and fill in. .env is gitignored — never commit real keys.
# API-Football (api-sports.io v3). Free tier ~100 requests/day.
API_FOOTBALL_KEY=

# Anthropic (Opus 4.8). The user normally supplies this in Settings instead.
ANTHROPIC_API_KEY=
